import 'package:flutter/material.dart';

class Eduauraa extends StatelessWidget {
  const Eduauraa({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}