import 'package:flutter/material.dart';

class HipiShorts extends StatelessWidget {
  const HipiShorts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
